/*
*
* # X
*  ---
*
*  ## Usage:
*
* `terraform init`
* `terraform plan`
* `terraform apply`
*
*/

// resource "random_pet" "bucket_name" {
//   length = 4
//   prefix = var.name
// }

// module "x" {
//   source      = "../../"
//   name        = var.name
//   environment = var.environment
// }